package com.foi.fitness.app.game.entities

import android.content.Context
import com.foi.fitness.app.R

class Block(context: Context, screenWidth: Int, screenHeight: Int) : Obstacle(context, R.drawable.block, screenWidth, screenHeight) {

}
